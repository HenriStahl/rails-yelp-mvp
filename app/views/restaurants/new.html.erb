<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
  <%= f.input :category %w(italian french belgian chinese) %>
  <%= f.button :submit %>
<% end %>
